name = "MaoDingA/moonfollow"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "timecode", "audio", "video", "foley", "footsteps" ]

preferred_target = "native"

description = "Detect footstep landings from tracked feet, derive SMPTE timecodes, and render a synced footstep SFX track."
